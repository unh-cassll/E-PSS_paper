# numpy >= 2 required (np.trapezoid, np.asin)
numpy>=2
xarray
pandas
netCDF4

scipy
scientimate

matplotlib
seaborn

# wavenumber/inverse-phase-speed spectra (coordinate="wavenumber"/"nu" in
# Arrays.compute()) are now merged into upstream main.
ewdm @ git+https://github.com/dspelaez/extended-wdm.git
epss @ git+https://github.com/unh-cassll/polarimetric-slope-sensing.git
